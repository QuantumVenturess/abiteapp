<%= link_to change_date_table_path(@table, date: prev_month(@date)), class: 'prev', remote: true do %>
  <div></div>
<% end %>
<%= link_to change_date_table_path(@table, date: next_month(@date)), class: 'next', remote: true do %>
  <div></div>
<% end %>
<div class="month">
  <%= @date.strftime('%B, %Y') %>
</div>
<%= calendar @date do |date| %>
  <% if date.month == @date.month %>
    <div class="date <%= 'selected' if @date == date %>" value="<%= date.strftime('%Y-%m-%d') %>">
      <%= date.day %>
    </div>
  <% end %>
<% end %>