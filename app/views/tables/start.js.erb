// Load all search results
$('.place_results').html('<%= j render partial: "places/search_results", 
  object: @results %>');

$('.lazy').lazyload({
  failure_limit: 50,
  skip_invisible: false
});
$('.lazyLoad').lazyload({
  event: 'load',
  failure_limit: 50
});